import { writable } from 'svelte/store';

export const pageTitle = writable('Home');
export const pageDescription = writable('Run your favorite IDE on Kubernetes');
export const pageUrl = writable('');
